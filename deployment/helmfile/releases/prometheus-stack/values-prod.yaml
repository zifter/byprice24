# Prometheus settings
prometheus:
  prometheusSpec:
    routePrefix: "/prometheus"

  ingress:
    hosts:
      - findprice.by
    paths:
      - "/prometheus(/|$)(.*)"

# Alertmanager settings
alertmanager:
  ingress:
    annotations:
      nginx.ingress.kubernetes.io/rewrite-target: /$2
    hosts:
      - findprice.by
    paths:
      - "/alertmanager(/|$)(.*)"

# Grafana settings
grafana:
  # TODO
  adminUser: admin
  adminPassword: admin

  grafana.ini:
    server:
      domain: findprice.by
      root_url: https://findprice.by:1080/grafana/
      serve_from_sub_path: true

  ingress:
    annotations:
      nginx.ingress.kubernetes.io/rewrite-target: /$2
    hosts:
      - findprice.by
    path: "/grafana(/|$)(.*)"
